import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    boxTitle: {
      alignItems: "center",
      marginTop:200,
    },
    textTitle: {
      fontSize:16,
      fontWeight:"bold"
    },
    tinyLogo: {
      width: 100,
      height: 100,
      marginBottom:20,
    }
  });
  
export default styles;